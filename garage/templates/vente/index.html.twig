
{% extends 'base.html.twig' %}

{% block title %}Vente index{% endblock %}

{% block body %}
    <style>
        .imageVente{
            display: flex;
            flex-direction: row;
            justify-content: center;
            margin-top: 5%;
        }
        .titleVente{
            background: cornflowerblue;
            padding: 8px 13% 8px 13%;
            font-size: 4vw;
            /* position: absolute; */
            margin-top: -4%;
            color: white;
        }
        .englobImg{
            display: flex;
            flex-direction: row;
            justify-content: center;
        }
        .textVente{
            display: flex;
            flex-direction: row;
            justify-content: center;
            text-align: justify;
            width: 40%
        }
        .englobcritere{
            display: flex;
            flex-direction: row;
            justify-content: center;
            margin-top: 2%;
        }
        .prix{
            text-align: right;
            width: 84%;
            margin-top: 2%;

        }
        .en{
            font-family: "Arial Black";
        }
        .englobButVente{
            display:flex;
            flex-direction: row;
            justify-content: space-around;
            margin-top: 2%;
        }
        .butVente{
            text-decoration: none;

        }
        .englobCreate{
            display:flex;
            flex-direction: row;
            justify-content: center;
            margin-top: 2%;
        }
    </style>
    <h1>Vente index</h1>

    <table class="table">
        <thead>

        </thead>
        <body>
        <h1 class="titleAccueil">Vente de Voiture</h1>
        {% for vente in ventes %}



                <div class="imageVente">
                    <img src="/image/{{ vente.image }}" alt="">

                </div>
        <div class="englobImg">  <div class="titleVente">{{ vente.titre }}</div></div>
            <div class="en">
          <div style="text-align: center; margin-top: 3%;">  <strong>Description technique: </strong></div>
            <div class="englobcritere"> <div class="textVente">{{ vente.content }}</div></div>
                <div>

                <div class="prix"><strong>Prix: </strong>{{ vente.prix }} Euro</div>
                <div class="englobButVente">
                    <a class="butVente" href="{{ path('vente_show', {'id': vente.id}) }}">show</a>
                    <a class="butVente" href="{{ path('vente_edit', {'id': vente.id}) }}">edit</a>
                </div>

        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </body>
    </table>

   <div class="englobCreate" ><a class="Crée" href="{{ path('vente_new') }}">Create new</a></div>
{% endblock %}
