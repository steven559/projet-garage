{% extends 'base.html.twig' %}

{% block title %}UserId{% endblock %}

{% block body %}
    <h1>UserId</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_id.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ user_id.jour }}</td>
            </tr>
            <tr>
                <th>HeurDepart</th>
                <td>{{ user_id.heurDepart }}</td>
            </tr>
            <tr>
                <th>HeurFin</th>
                <td>{{ user_id.heurFin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_id_index') }}">back to list</a>

    <a href="{{ path('user_id_edit', {'id': user_id.id}) }}">edit</a>

    {{ include('user_id/_delete_form.html.twig') }}
{% endblock %}
