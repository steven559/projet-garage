{% extends 'base.html.twig' %}

{% block title %}Edit UserId{% endblock %}

{% block body %}
    <h1>Edit UserId</h1>

    {{ include('user_id/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_id_index') }}">back to list</a>

    {{ include('user_id/_delete_form.html.twig') }}
{% endblock %}
