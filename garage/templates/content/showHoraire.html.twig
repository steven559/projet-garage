{% extends 'base.html.twig' %}

{% block title %}Content{% endblock %}

{% block body %}
    <h1>Content</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ content.id }}</td>
        </tr>
        <tr>
            <th>Content</th>
            <td>{{ content.content }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('content_index') }}">back to list</a>

    <a href="{{ path('horaire_edit', {'id': content.id}) }}">edit</a>

    {{ include('content/_deleteHoraire_form.html.twig') }}
{% endblock %}