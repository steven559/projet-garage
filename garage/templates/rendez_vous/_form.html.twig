<style>

</style>
<!-- Include English language -->


<div>{{ form_start(form)  }}</div>
<div>   {{ form_widget(form) }}</div>

<button class="btn btn-primary" style="margin-left: 3%; margin-top: 1%;">valider</button>

<div id="push">{{ form_end(form) }}</div>





<script>// Initialization
    $('.datepicker-here').datepicker({
        language: 'fr',
        minDate: new Date()
        // Now can select only dates, which goes after today
    });
    var disabledDays = [0, 6];


//function qui permet d'enlever les jour passer

    $('.datepicker-here').datepicker({
        language: 'fr',
        onRenderCell: function (date, cellType) {
            if (cellType == 'day') {
                var day = date.getDay(),
                    isDisabled = disabledDays.indexOf(day) != -1;

                return {
                    disabled: isDisabled
                }
            }
        }
    })
    $('.datepicker-here').datepicker({
        timepicker: true,
        language: 'fr',

        minHours: 9 ,
        maxHours: 19,
        onSelect: function (fd, d, picker) {
            // Do nothing if selection was cleared
            if (!d) return;

            var day = d.getDay();

            // Trigger only if date is changed
            if (prevDay != undefined && prevDay == day) return;
            prevDay = day;

            // If chosen day is Saturday or Sunday when set
            // hour value for weekends, else restore defaults
            if (day == 6 || day == 0) {
                picker.update({
                    minHours: 10,
                    maxHours: 16
                })
            } else {
                picker.update({
                    minHours: 9,
                    maxHours: 18
                })
            }
        }
    });



</script>
